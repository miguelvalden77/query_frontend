

export default interface event {
    target: {
        name: string,
        value: string
    }
}

export default interface data {
    username: string,
    password: string,
    email?: string 
}
